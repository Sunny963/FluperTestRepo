<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="@dimen/_12sdp">

    <EditText
        android:id="@+id/et_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Name"
        android:inputType="text"
        android:lines="1"
        android:padding="@dimen/_9sdp"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/et_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter description"
        android:inputType="text"
        android:lines="1"
        android:padding="@dimen/_9sdp"
        app:layout_constraintTop_toBottomOf="@+id/et_name" />

    <EditText
        android:id="@+id/et_sale_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Sale Price"
        android:inputType="text"
        android:lines="1"
        android:padding="@dimen/_9sdp"
        app:layout_constraintTop_toBottomOf="@+id/et_description" />

    <EditText
        android:id="@+id/et_regular_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Regular Price"
        android:inputType="text"
        android:lines="1"
        android:padding="@dimen/_9sdp"
        app:layout_constraintTop_toBottomOf="@+id/et_sale_price" />

    <Button
        android:id="@+id/btn_update"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_30sdp"
        android:text="Update"
        app:layout_constraintTop_toBottomOf="@+id/et_regular_price" />

</androidx.constraintlayout.widget.ConstraintLayout>